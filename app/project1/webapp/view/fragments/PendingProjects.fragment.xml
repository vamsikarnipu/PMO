<core:FragmentDefinition xmlns:core="sap.ui.core"
                         xmlns="sap.m"
                         xmlns:mdc="sap.ui.mdc"
                         xmlns:table="sap.ui.mdc.table">

    <VBox class="sapUiSmallMargin">

        <mdc:Table
            id="PendingProjects"
            header="Pending Projects Report"
            autoBindOnInit="true"
            width="100%"
            p13nMode="Column,Sort,Filter,Group"
            selectionMode="Multi"
            delegate='{
                "name": "project1/delegate/PendingProjectsTableDelegate",
                "payload": {
                    "collectionPath": "Projects"
                }
            }'
            showRowCount="true"
            threshold="1000"
            enableExport="true"
            enableAutoColumnWidth="true"
            selectionChange="onSelectionChange">

            <mdc:type>
                <table:ResponsiveTableType />
            </mdc:type>

            <!-- ✅ Heading and Buttons in actions toolbar -->
            <mdc:actions>
                <OverflowToolbar>
                    <!-- Refresh Button -->
                    <Button
                        id="refreshPendingButton"
                        text="Refresh"
                        icon="sap-icon://refresh"
                        press="onRefreshPendingProjects"
                    />
                    <!-- Export Button -->
                    <Button
                        id="exportPendingButton"
                        text="Export"
                        icon="sap-icon://excel-attachment"
                        press="onExportPendingProjects"
                    />
                    <!-- Filter Button -->
                    <Button
                        id="filterPendingButton"
                        text="Filter"
                        icon="sap-icon://filter"
                        press="onFilterPendingProjects"
                    />
                </OverflowToolbar>
            </mdc:actions>
            <mdc:copyProvider>
					<plugins.CopyProvider id="copyProvider_pending"/>
			</mdc:copyProvider>

        </mdc:Table>
    </VBox>
</core:FragmentDefinition>
