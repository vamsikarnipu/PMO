<core:FragmentDefinition xmlns:core="sap.ui.core"
                         xmlns="sap.m"
                         xmlns:mdc="sap.ui.mdc"
                         xmlns:table="sap.ui.mdc.table">

    <VBox class="sapUiSmallMargin">

        <mdc:Table
            id="BenchReport"
            header="Bench Report - Available Employees"
            autoBindOnInit="true"
            width="100%"
            p13nMode="Column,Sort,Filter,Group"
            selectionMode="Multi"
            delegate='{
                "name": "project1/delegate/BenchReportTableDelegate",
                "payload": {
                    "collectionPath": "EmployeeAllocation"
                }
            }'
            showRowCount="true"
            threshold="1000"
            enableExport="true"
            enableAutoColumnWidth="true"
            selectionChange="onSelectionChange">

            <mdc:type>
                <table:ResponsiveTableType />
            </mdc:type>

            <!-- ✅ Heading and Buttons in actions toolbar -->
            <mdc:actions>
                <OverflowToolbar>
                    <!-- Refresh Button -->
                    <Button
                        id="refreshBenchButton"
                        text="Refresh"
                        icon="sap-icon://refresh"
                        press="onRefreshBenchReport"
                    />
                    <!-- Export Button -->
                    <Button
                        id="exportBenchButton"
                        text="Export"
                        icon="sap-icon://excel-attachment"
                        press="onExportBenchReport"
                    />
                </OverflowToolbar>
            </mdc:actions>
            <mdc:copyProvider>
					<plugins.CopyProvider id="copyProvider_bench"/>
			</mdc:copyProvider>

        </mdc:Table>
    </VBox>
</core:FragmentDefinition>
