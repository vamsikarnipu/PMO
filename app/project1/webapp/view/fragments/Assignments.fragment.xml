<core:FragmentDefinition xmlns:core="sap.ui.core"
                         xmlns="sap.m"
                         xmlns:mdc="sap.ui.mdc"
                         xmlns:table="sap.ui.mdc.table">

    <VBox class="sapUiSmallMargin">

        <mdc:Table
            id="Assignments"
            header="Assignments"
            autoBindOnInit="true"
            width="100%"
            p13nMode="Column,Sort,Filter,Group"
            selectionMode="Multi"
            delegate='{
                "name": "project1/delegate/AssignmentsTableDelegate",
                "payload": {
                    "collectionPath": "Assignments"
                }
            }'
            showRowCount="true"
            threshold="1000"
            enableExport="true"
            enableAutoColumnWidth="true"
            selectionChange="onSelectionChange">

            <mdc:type>
                <table:ResponsiveTableType />
            </mdc:type>

            <!-- ✅ Heading and Buttons in actions toolbar -->
            <mdc:actions>
                <OverflowToolbar>
                    <!-- Save and Cancel Buttons -->
                    <HBox >
                        <Button
                            id="saveButton_assign"
                            text="Save"
                            type="Accept"
                            enabled="false"
                            press="onSaveButtonPress"
                        />
                        <Button
                            id="cancelButton_assign"
                            text="Cancel"
                            type="Reject"
                            enabled="false"
                            press="onCancelButtonPress"
                            class="sapUiSmallMarginBegin"
                        />
                    </HBox>
                    <!-- Action Buttons -->
                    <Button icon="sap-icon://add" id="btnAdd_assign" tooltip="Add" press="onAdd"/>
                    <Button icon="sap-icon://delete" id="btnDelete_assign" tooltip="Delete" press="onDeletePress" enabled="false"/>
                    <Button icon="sap-icon://edit" id="btnEdit_assign" tooltip="Edit" press="onEditPress" enabled="false"/>
                    <!-- Business Actions -->
                    <Button 
                        icon="sap-icon://play" 
                        id="btnStartAssignment" 
                        tooltip="Start Assignment" 
                        press="onStartAssignment"
                        type="Emphasized"
                    />
                    <Button 
                        icon="sap-icon://stop" 
                        id="btnCloseAssignment" 
                        tooltip="Close Assignment" 
                        press="onCloseAssignment"
                        type="Reject"
                    />
                </OverflowToolbar>
            </mdc:actions>
            <mdc:copyProvider>
					<plugins.CopyProvider id="copyProvider_assign"/>
			</mdc:copyProvider>

        </mdc:Table>
    </VBox>
</core:FragmentDefinition>
